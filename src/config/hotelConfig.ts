/**
 * CORE HOTEL IDENTITY
 * Change the hotel's name, tagline, and positioning copy here — nothing
 * else in the codebase needs to be touched.
 */
import type { HotelInfo } from "@/types/content";

export const hotelInfo: HotelInfo = {
  name: "COSMOS",
  legalName: "Cosmos Hospitality Pvt. Ltd.",
  tagline: "A quiet address in the heart of the city",
  positioningStatement:
    "COSMOS is an independently-run city hotel built around considered design, attentive service, and genuine Indian hospitality.",
  foundedYear: 2024,
  isPlaceholderInfo: true,
};
